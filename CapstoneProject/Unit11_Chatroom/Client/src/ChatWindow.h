#pragma once
#include <curses.h>
#include "UIWindow.h"

// Class for main chat window
class ChatWindow : public UIWindow
{
public:
	ChatWindow();
	virtual ~ChatWindow() override;

private:
};